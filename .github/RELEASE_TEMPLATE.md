# AudioLabEditor $TAG

## 📦 Assets

- **Linux**: `audiolab-editor-linux-x86_64` — binary PyInstaller onefile
- **Windows**: `audiolab-editor-windows-x86_64.exe` — binary PyInstaller onefile
- **macOS**: `audiolab-editor-macos-x86_64` — binary PyInstaller onefile

## 🚀 Instalação

### Linux
```bash
chmod +x audiolab-editor-linux-x86_64
./audiolab-editor-linux-x86_64
```

### Windows
Execute `audiolab-editor-windows-x86_64.exe` diretamente.

### macOS
```bash
chmod +x audiolab-editor-macos-x86_64
./audiolab-editor-macos-x86_64
```

Para integração ao sistema (atalho, PATH), use o script correspondente em `scripts/`.

---

*Release gerada automaticamente pelo GitHub Actions.*
