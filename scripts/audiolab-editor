#!/usr/bin/env bash
set -euo pipefail
HERE="$(cd "$(dirname "$0")" && pwd)"
ROOT="$(cd "$HERE/.." && pwd)"
PYTHONPATH="$ROOT/src:$ROOT${PYTHONPATH:+:}$PYTHONPATH" exec python3 "$ROOT/src/presentation/main.py" "$@"
